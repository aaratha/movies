import classNames from 'classnames';
import React from 'react';

export interface SearchProps {
    className?: string;
}

export const search = ({ className }: SearchProps) => {
    <div className='border-co2 border'>
       <p>Title
       </p>
    </div>
};